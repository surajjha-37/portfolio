const heading = React.createElement(
  "h1",
  { id: "react" },
  "Hello SRG from react"
);

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(heading);

// Access-Control-Allow-Origin: https://example.com
